import SideBarData from "@/constants/sideBarData";
import {
    ACCOUNT_SETTING,
    CONTACT_BOOK,
    HOME_PATH,
    PAYMENT_METHOD,
    SIGN_IN_PATH,
    SIGN_OUT_PATH
} from "@/constants/routingPath";

module.exports = {
    SideBarData,
    HOME_PATH,
    SIGN_IN_PATH,
    SIGN_OUT_PATH,
    CONTACT_BOOK,
    PAYMENT_METHOD,
    ACCOUNT_SETTING
}